import { BaseCommand } from '@yarnpkg/cli';
import { Configuration } from '@yarnpkg/core';
import { PortablePath } from '@yarnpkg/fslib';
import { Usage } from 'clipanion';
export default class NpmLoginCommand extends BaseCommand {
    scope?: string;
    publish: boolean;
    static usage: Usage;
    execute(): Promise<0 | 1>;
}
export declare function getRegistry({ scope, publish, configuration, cwd }: {
    scope?: string;
    publish: boolean;
    configuration: Configuration;
    cwd: PortablePath;
}): Promise<string>;
