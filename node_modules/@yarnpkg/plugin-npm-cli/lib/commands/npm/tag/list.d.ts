import { BaseCommand } from '@yarnpkg/cli';
import { Configuration, Ident } from '@yarnpkg/core';
import { Usage } from 'clipanion';
export default class NpmTagListCommand extends BaseCommand {
    package?: string;
    json: boolean;
    static usage: Usage;
    execute(): Promise<void>;
}
export declare function getDistTags(ident: Ident, configuration: Configuration): Promise<Record<string, string>>;
