import { BaseCommand } from '@yarnpkg/cli';
import { Usage } from 'clipanion';
import * as npmAuditTypes from '../../npmAuditTypes';
export default class AuditCommand extends BaseCommand {
    all: boolean;
    recursive: boolean;
    environment: npmAuditTypes.Environment;
    json: boolean;
    severity: npmAuditTypes.Severity;
    static usage: Usage;
    execute(): Promise<0 | 1>;
}
