/// <reference types="node" />
import { FetchOptions, Locator, Ident } from '@yarnpkg/core';
import { PortablePath, ZipFS } from '@yarnpkg/fslib';
export declare function parseSpec(spec: string): {
    parentLocator: Locator | null;
    path: PortablePath;
};
export declare function makeSpec({ parentLocator, path, folderHash, protocol }: {
    parentLocator: Locator | null;
    path: string;
    folderHash?: string;
    protocol: string;
}): string;
export declare function makeLocator(ident: Ident, { parentLocator, path, folderHash, protocol }: Parameters<typeof makeSpec>[number]): Locator;
export declare function makeArchiveFromLocator(locator: Locator, { protocol, fetchOptions, inMemory }: {
    protocol: string;
    fetchOptions: FetchOptions;
    inMemory?: boolean;
}): Promise<ZipFS>;
export declare function makeBufferFromLocator(locator: Locator, { protocol, fetchOptions }: {
    protocol: string;
    fetchOptions: FetchOptions;
}): Promise<Buffer>;
