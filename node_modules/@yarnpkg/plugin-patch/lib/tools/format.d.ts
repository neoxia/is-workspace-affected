import { Report, Configuration } from '@yarnpkg/core';
import { Hunk } from './parse';
export declare function reportHunk(hunk: Hunk, { configuration, report }: {
    configuration: Configuration;
    report: Report;
}): void;
